@layout('layouts/main')
@set('title', 'Détail')

@section('body')
<main class="m-0 h-full flex justify-center items-center">
    <div class="bg-white p-8 rounded shadow-md w-1/2">
        <h2 class="text-2xl font-bold mb-4">Détails de la demande de whitelist</h2>

        <div class="mb-4">
            <strong>Nom RP:</strong> {{ requestWl.lastname }}
        </div>

        <div class="mb-4">
            <strong>Prénom RP:</strong> {{ requestWl.firstname }}
        </div>

        <div class="mb-4">
            <strong>Job:</strong> {{ requestWl.job }}
        </div>

        <div class="mb-4">
            <strong>Type de Job:</strong> {{ requestWl.jobType }}
        </div>

        <div class="mb-4">
            <strong>Date d'anniversaire:</strong> {{ requestWl.birthday.toFormat('yyyy-MM-dd') }}
        </div>

        <div class="mb-4 overflow-auto max-h-40">
            <strong>Background du personnage:</strong>
            <p class="whitespace-pre-line">{{ requestWl.background }}</p>
        </div>

        <div class="mt-8 flex justify-between items-center">
            <a href="/douane" class="text-blue-500 hover:underline">Retour à la liste</a>
            <div class="space-x-4 flex items-center">
            <form action="{{ route('douane.reject', { id: requestWl.id }) }}" method="post">
              <!-- Ajoutez des champs supplémentaires si nécessaire -->
                              <button class="p-2 bg-red-500 text-white rounded-md hover:bg-red-600" type="submit">Refuser la candidature</button>
            </form>

            <form action="{{ route('douane.accept', { id: requestWl.id }) }}" method="post">
                          <!-- Ajoutez des champs supplémentaires si nécessaire -->
               <button class="p-2 bg-green-500 text-white rounded-md hover:bg-green-600">Passer à l'entretien vocal</button>

            </form>


            </div>
        </div>
    </div>
</main>
@end
